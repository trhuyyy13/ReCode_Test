# Core training / HF stack (Py3.12-friendly)
accelerate>=0.28.0,<2.0
transformers>=4.46.0,<5.0
trl>=0.13.0,<0.16.0
peft>=0.11.0,<1.0
datasets>=2.18.0,<3.0

# Data / numerics (must be new for Py3.12 wheels)
numpy>=1.26.4,<3.0
pandas>=2.2.0,<3.0
pyarrow>=15.0.0

# Utilities
codetiming==1.0.0
dill>=0.3.8
hydra-core==1.3.2
pybind11>=2.11.1
pylatexenc==2.10
packaging>=20.0

# Tracking / serving
wandb>=0.16.0
uvicorn==0.23.2
fastapi==0.103.2
tensorboardX>=2.6.2

# Ray / RL utils
ray[default]>=2.9.0,<3.0
tensordict>=0.6.0,<0.7.0

# Project-specific
latex2sympy2_extended==0.1.0
math_verify==0.1.0

# Dev-only (optional on Kaggle)
pre-commit==3.3.3

# ❌ Avoid on Kaggle Py3.12 unless you know exact matching wheels:
# flash-attn  (often triggers source build)
# torchdata   (old pins break on Py3.12; install only if you truly need it)
# liger-kernel (often needs compilation / specific torch)